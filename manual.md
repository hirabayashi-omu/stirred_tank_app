# AgitatorSim ユーザーマニュアル

本マニュアルでは、攪拌動力シミュレーションアプリ（AgitatorSim）の各画面（UI）と機能の使い方について解説します。

---

## 1. メイン画面（パラメータ入力・計算）
メイン画面（`index.html`）は、攪拌槽の諸条件を入力し、所要動力を計算するためのメインインターフェースです。大きく分けて以下のパネルで構成されています。

### 1.1 槽のジオメトリ設定パネル
攪拌槽の形状に関する設定を行います。
*   **槽径 $D_T$ [m]**: 攪拌槽の内径を入力します。
*   **槽底形状**: 槽の底の形を「平底 (Flat)」「皿底 (Dish)」「半楕円底 (Semi-Elliptical)」「半球底 (Hemispherical)」から選択します。
*   **邪魔板（バッフル）**: 邪魔板の有無をチェックボックスで切り替えます。
    *   有効にした場合、邪魔板の「幅 $B_w$ [m]」と「枚数 $n_B$」を指定します。

### 1.2 インペラ（攪拌翼）のジオメトリ設定パネル
使用する攪拌翼の形状と配置を設定します。
*   **翼種**: 「フラットパドル/タービン」「傾斜パドル」「プロペラ/ファウドラー」から選択します。
*   **インペラ径 $d$ [m]**: 攪拌翼の直径を入力します。
*   **翼幅 $b$ [m]**: 翼の上下方向の幅を入力します。
*   **ピッチ角 $\theta$ [deg]**: 傾斜パドルなどの傾き角度を入力します（フラットの場合は90度）。
*   **翼枚数 $n_p$**: 1段あたりの羽根の枚数（例: 2枚、4枚など）を入力します。
*   **段数 $n_{stage}$**: 翼を何段設置するかを指定します（多段翼の場合は2以上）。
*   **クリアランス $C$ [m]**: 槽底から一番下のインペラ中心までの高さを設定します。

### 1.3 物性・操作条件パネル
攪拌液の性質や回転速度を入力します。
*   **密度 $\rho$ [kg/m³]**: 液体の密度を入力します。水の場合は約1000です。
*   **回転数 $N$ [rpm]**: インペラの1分間あたりの回転数を入力します。
*   **レオロジーモデル**: 液体の粘性特性を以下の5種類から選択します。
    *   **Newtonian (ニュートン流体)**: 水や油などの一般的な流体。粘度 $\mu$ を入力します。
    *   **Power-law (べき乗則流体)**: 擬塑性流体など。粘性係数 $K$ と 流動挙動指数 $n$ を入力します。
    *   **Bingham (ビンガム流体)**: 降伏応力 $\tau_y$ と 塑性粘度 $\eta_p$ を入力します。
    *   **Herschel-Bulkley (HB流体)**: 降伏応力 $\tau_y$、粘度係数 $K$、流動指数 $n$ を入力します。
    *   **Casson (カソン流体)**: 降伏値 $\tau_0$ と カソン粘度 $\eta_c$ を入力します。

### 1.4 E型粘度計データ連携パネル（CSV読み込み）
測定した粘度データ（CSV）を取り込み、最適なパラメータを自動計算します。
*   **ファイルを選択**: E型粘度計で出力されたCSVファイルを選択し、「読み込む」ボタンを押します。
*   **解析結果一覧**: 各非ニュートンモデル（Power-law, Bingham, HB, Casson）へのフィッティング（回帰分析）結果がテーブルで表示されます。
    *   **R² (決定係数)**: 1に近いほどデータによく適合しています。
    *   **「選択」ボタン**: 使いたいモデルの「選択」ボタンを押すと、そのモデルと算出されたパラメータが「1.3 物性・操作条件パネル」へ自動的に反映されます。
*   **選択をクリア**: 反映したCSVデータを解除し、手動入力モードに戻ります。

### 1.5 キャバーンモデル設定パネル
降伏応力（$\tau_y$ または $\tau_0$）を持つ流体において発生する「死水域（キャバーン）」の予測モデルを選択します。
*   **Elson 球形モデル**: キャバーンを球体と仮定します。
*   **Elson 円筒形モデル**: キャバーンを円筒形と仮定します。
*   **Amanullah トーラス形モデル**: 軸流翼（プロペラなど）で発生しやすいドーナツ状のキャバーンを想定します。

### 1.6 計算結果パネル（画面下部・右側）
入力されたパラメータに基づき、リアルタイムに計算結果が表示されます。
*   **有効Re数 ($Re_{eff}$)**: 非ニュートン流体の見かけ粘度を考慮したレイノルズ数。
*   **見かけ粘度 ($\mu_{eff}$)**: Metzner-Otto法により算出された槽内の平均的な粘度。
*   **動力数 ($Np$)**: 攪拌における抵抗係数。
*   **最大動力数 ($Np_{Max}$)**: 邪魔板効果が最大に発揮された場合の動力数限界値。
*   **所要動力 ($P$)**: モーターに必要な動力（W および kW 表示）。
*   **CSVエクスポート**: 現在の入力条件と計算結果をCSVファイルとしてダウンロードします。

---

## 2. 槽内図面ビューア（`diagram.html`）
メイン画面から「槽内図面の表示 (diagram.html)」ボタンをクリックすると開く可視化画面です。

*   **図面描画エリア**: 入力されたパラメータ（槽径、翼径、液深、クリアランスなど）がスケール通りにSVG形式で描画されます。
*   **多段翼の表現**: メイン画面で段数（$n_{stage}$）を増やすと、その数だけ翼が積み重なって表示されます。
*   **邪魔板の表現**: 邪魔板が有効な場合、槽の側面に緑色の矩形で表示されます。
*   **キャバーン（死水域）の可視化**: 
    *   降伏応力を持つ流体モデル（Bingham, HB, Casson）が選択されている場合、各羽根の周囲にピンク色の点線でキャバーン領域が表示されます。
    *   テキストラベルにはキャバーン径（$Dc$）が記載されます。
*   **戻るボタン**: 「シミュレーターへ戻る」ボタンでメイン画面へ戻ります。

---

## 3. 推算式・算出根拠ページ（`formula.html`）
メイン画面から「推算式の確認」ボタンをクリックすると開く、理論・文献背景の解説画面です。

*   **攪拌動力の相関式**: 亀井・平岡・加藤らによって提案された層流域から乱流域までをカバーする動力数推算式（$Np_0$ や $Np_{Max}$）の数式が記載されています。
*   **非ニュートン流体の取り扱い**: Metzner-Ottoの仮定（$\dot{\gamma}_{avg} = k_s \cdot n$）に基づく有効レイノルズ数の算出方法が解説されています。
*   **キャバーン推算理論**: ElsonらのモデルやAmanullahらのモデルに基づくキャバーン径（$D_c$）の算出式が掲載されています。
*   **参考文献**: 各理論の出典となった化学工学論文集などの原著論文が記載されており、DOIリンクから直接論文ページへアクセス可能です。

---

> [!TIP]
> **推奨する操作フロー**
> 1. まず「槽・翼のジオメトリ」を設定します。
> 2. 次に、もしE型粘度計のデータ（CSV）があれば読み込み、最適なレオロジーモデルを「選択」します（なければ手動入力）。
> 3. 回転数（$N$）を調整しながら「所要動力」や「キャバーン径」が要件を満たすか確認します。
> 4. 「槽内図面の表示」を開き、キャバーンが槽壁面まで届いているか（全体が流動しているか）、翼の配置バランスが良いかを視覚的に確認します。
